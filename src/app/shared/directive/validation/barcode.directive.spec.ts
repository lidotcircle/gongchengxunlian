import { BarcodeDirective } from './barcode.directive';

describe('BarcodeDirective', () => {
  it('should create an instance', () => {
    const directive = new BarcodeDirective();
    expect(directive).toBeTruthy();
  });
});
